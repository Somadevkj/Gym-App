
  # Gym app

  This is a Gym Workout Planner app that generates personalized training plans based on your goals, experience, and physical limitations.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  